#include <stdio.h>

int main(){
    char str[100];
    scanf("%s", str);
    if (char = R){
        printf("Stop");
    }else if (char = Y){
        printf("Slow Down");
    } else if (char = G){
        printf("Go");
    } else {
        printf("Invalid input");
    }
}