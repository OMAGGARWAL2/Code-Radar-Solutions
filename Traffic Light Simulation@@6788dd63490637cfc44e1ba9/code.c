#include <stdio.h>

int main(){
    char str[100];
    scanf("%s", &str);
    if (str = R){
        printf("Stop");
    }else if (str = Y){
        printf("Slow Down");
    } else if (str = G){
        printf("Go");
    } else {
        printf("Invalid input");
    }
}